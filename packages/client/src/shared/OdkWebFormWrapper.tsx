import React from 'react'
import {OdkWebForm, OdkWebFormProps} from '@infoportal/odk-web-form-wrapper'

export const OdkWebFormWrapper = (props: OdkWebFormProps) => {
  return <OdkWebForm {...props} />
}
